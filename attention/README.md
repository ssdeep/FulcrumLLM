# Multi-head Attention Mechanism
Multi-head attention mechanism class in this module is designed to produce context vector 
given an input embedding. 
The class can be configured to have a fixed number of heads and defined context size 
and configurable dropping factor for randomized modulation of the causal mask used.